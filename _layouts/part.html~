---
layout: default
---
<div class="post">
  
  <header class="post-header">
    <h1 class="post-title">Chapter {{page.chapter }} {{ page.title }}</h1>
    <p class="post-meta"{% if page.author %} • {{ page.author }}{% endif %}{% if page.meta %} • {{ page.meta }}{% endif %}</p>
       {% if page.notes %}
       <ul>
	 {% for note in page.notes %}
	 {% if note %}
	 <li>{{ note | append: ' ' | markdownify }}</li>
	 {% endif %}
	 {% endfor %}
       </ul>
       {% endif %}
  </header>

  <article class="post-content">
    {{ content }}
    {% if page.summary %}
    <h2>Summary</h2>
    <ul>
      {% for summary in page.summary %}
      {% if summary %}
      <li>{{ summary | append: ' ' | markdownify }}</li>
      {% endif %}
    {% endfor %}
    </ul>
    {% endif %}
  </article>
  

</div>
